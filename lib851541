local lib = {}

local RunService = game:GetService("RunService")   
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local Debris = game:GetService("Debris")

local tweenInfo = TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut)

lib.DraggingEnabled = true
lib.CreatedUI = nil
lib.Templates = nil
lib.LibGUID = HttpService:GenerateGUID(false)

lib.DragList = {}
lib.Pages = {}
lib.Tabs = {}
lib.Blocks = {}
lib.Buttons = {}
lib.SelectedTab = ""

function lib.newDrag(frame, storeTo, id)
	assert(typeof(storeTo) == "table", "storeTo must be a table")
	assert(id ~= nil, "Drag ID is required")

	if storeTo[id] then
		lib.RemoveDrag(storeTo, id)
	end

	local dragData = {
		dragging = false,
		dragInput = nil,
		dragStart = nil,
		startPos = nil,
		connections = {}
	}

	local function update(position)
		if not lib.DraggingEnabled then return end
		if not dragData.dragging then return end

		local delta = position - dragData.dragStart
		frame.Position = UDim2.new(
			dragData.startPos.X.Scale,
			dragData.startPos.X.Offset + delta.X,
			dragData.startPos.Y.Scale,
			dragData.startPos.Y.Offset + delta.Y
		)
	end

	dragData.connections.began = frame.InputBegan:Connect(function(input)
		if not lib.DraggingEnabled then return end

		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			dragData.dragging = true
			dragData.dragInput = input
			dragData.dragStart = input.Position
			dragData.startPos = frame.Position

			dragData.connections.touchMove = input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.Change then
					update(input.Position)
				end
			end)
		end
	end)

	dragData.connections.mouseMove = UserInputService.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement then
			update(input.Position)
		end
	end)

	dragData.connections.ended = frame.InputEnded:Connect(function(input)
		if input == dragData.dragInput then
			dragData.dragging = false
			dragData.dragInput = nil

			if dragData.connections.touchMove then
				dragData.connections.touchMove:Disconnect()
				dragData.connections.touchMove = nil
			end
		end
	end)

	storeTo[id] = dragData
end

function lib.delDrag(storeTo, id)
	local dragData = storeTo[id]
	if not dragData then return end

	for _, connection in pairs(dragData.connections) do
		if connection and connection.Disconnect then
			connection:Disconnect()
		end
	end

	storeTo[id] = nil
end

function lib.toggleDrag()
	lib.DraggingEnabled = not lib.DraggingEnabled
end

function lib.createHUD()
    local PK_Alpha = Instance.new("ScreenGui") do
        PK_Alpha.Name = lib.LibGUID
        PK_Alpha.ResetOnSpawn = false
        PK_Alpha.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    end

    local Main = Instance.new("Frame",PK_Alpha) do
        Main:SetAttribute("Key","MainBackground")
        Main:SetAttribute("Property","BackgroundColor3")
        Main.Name = "Main"
        Main.AnchorPoint = Vector2.new(0.5,0.5)
        Main.BackgroundColor3 = Color3.new(0,0,0)
        Main.BackgroundTransparency = 0.1
        Main.Position = UDim2.new(0.5,0,0.5,0)
        Main.Size = UDim2.new(0.4,0,0.5,0)
    end

    local UIScale = Instance.new("UIScale",Main)
    local UIAspectRatioConstraint = Instance.new("UIAspectRatioConstraint",Main) do
        UIAspectRatioConstraint.AspectRatio = 1.5
    end
    local UICorner = Instance.new("UICorner",Main) do
        UICorner.BottomLeftRadius = UDim.new(0.05,0)
        UICorner.BottomRightRadius = UDim.new(0.05,0)
        UICorner.CornerRadius = UDim.new(0.05,0)
        UICorner.TopLeftRadius = UDim.new(0.05,0)
        UICorner.TopRightRadius = UDim.new(0.05,0)
    end
    local UIStroke = Instance.new("UIStroke",Main) do
        UIStroke:SetAttribute("Key","Stroke")
        UIStroke:SetAttribute("Property","Color")
        UIStroke.BorderStrokePosition = Enum.BorderStrokePosition.Center
        UIStroke.Color = Color3.new(0.071,0.071,0.071)
        UIStroke.Thickness = 5
        UIStroke.Transparency = 0.1
    end
    local Topbar = Instance.new("CanvasGroup",Main) do
        Topbar.Name = "Topbar"
        Topbar.BackgroundColor3 = Color3.new(1,1,1)
        Topbar.BackgroundTransparency = 1
        Topbar.BorderColor3 = Color3.new(0,0,0)
        Topbar.BorderSizePixel = 0
        Topbar.Size = UDim2.new(1,0,1,0)
    end

    local UICorner_1 = Instance.new("UICorner",Topbar) do
        UICorner_1.BottomLeftRadius = UDim.new(0.05,0)
        UICorner_1.BottomRightRadius = UDim.new(0.05,0)
        UICorner_1.CornerRadius = UDim.new(0.05,0)
        UICorner_1.TopLeftRadius = UDim.new(0.05,0)
        UICorner_1.TopRightRadius = UDim.new(0.05,0)
    end
    local Container = Instance.new("Frame",Topbar) do
        Container.Name = "Container"
        Container.BackgroundColor3 = Color3.new(0.118,0.118,0.118)
        Container.BorderColor3 = Color3.new(0,0,0)
        Container.BorderSizePixel = 0
        Container.Size = UDim2.new(1,0,0.075,0)
    end

    local Logo = Instance.new("ImageLabel",Container) do
        Logo.Name = "Logo"
        Logo.AnchorPoint = Vector2.new(0.5,0.5)
        Logo.BackgroundColor3 = Color3.new(1,1,1)
        Logo.BackgroundTransparency = 1
        Logo.BorderColor3 = Color3.new(0,0,0)
        Logo.BorderSizePixel = 0
        Logo.Image = "rbxassetid://132380102310679"
        Logo.Position = UDim2.new(0.5,0,0.5,0)
        Logo.ScaleType = Enum.ScaleType.Fit
        Logo.Size = UDim2.new(1,0,1.5,0)
    end
    local Close = Instance.new("TextButton",Container) do
        Close.Name = "Close"
        Close.AnchorPoint = Vector2.new(1,0.5)
        Close.BackgroundColor3 = Color3.new(1,1,1)
        Close.BackgroundTransparency = 1
        Close.BorderColor3 = Color3.new(0,0,0)
        Close.BorderSizePixel = 0
        Close.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Close.Position = UDim2.new(0.99,0,0.5,0)
        Close.Size = UDim2.new(0.95,0,0.95,0)
        Close.Text = "×"
        Close.TextColor3 = Color3.new(1,1,1)
        Close.TextScaled = true
        Close.TextSize = 14
        Close.TextWrapped = true
    end

    local UICorner_2 = Instance.new("UICorner",Close)
    local UIAspectRatioConstraint_1 = Instance.new("UIAspectRatioConstraint",Close)
    local Min = Instance.new("TextButton",Container) do
        Min.Name = "Min"
        Min.AnchorPoint = Vector2.new(1,0.5)
        Min.BackgroundColor3 = Color3.new(1,1,1)
        Min.BackgroundTransparency = 1
        Min.BorderColor3 = Color3.new(0,0,0)
        Min.BorderSizePixel = 0
        Min.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Min.Position = UDim2.new(0.94,0,0.5,0)
        Min.Size = UDim2.new(0.95,0,0.95,0)
        Min.Text = "–"
        Min.TextColor3 = Color3.new(1,1,1)
        Min.TextScaled = true
        Min.TextSize = 14
        Min.TextWrapped = true
    end

    local UICorner_3 = Instance.new("UICorner",Min)
    local UIAspectRatioConstraint_2 = Instance.new("UIAspectRatioConstraint",Min)
    local UIStroke_1 = Instance.new("UIStroke",Container) do
        UIStroke_1.Color = Color3.new(1,1,1)
    end
    local Glow = Instance.new("UIShadow",Main) do
        Glow.Name = "Glow"
        Glow.BlurRadius = UDim.new(0,20)
        Glow.Color = Color3.new(1,1,1)
        Glow.Transparency = 0.5
    end
    local Container_1 = Instance.new("Frame",Main) do
        Container_1.Name = "Container"
        Container_1.BackgroundColor3 = Color3.new(1,1,1)
        Container_1.BackgroundTransparency = 1
        Container_1.BorderColor3 = Color3.new(0,0,0)
        Container_1.BorderSizePixel = 0
        Container_1.Position = UDim2.new(0,0,0.075,0)
        Container_1.Size = UDim2.new(1,0,0.925,0)
    end

    local Tabs = Instance.new("Frame",Container_1) do
        Tabs.Name = "Tabs"
        Tabs.BackgroundColor3 = Color3.new(1,1,1)
        Tabs.BackgroundTransparency = 1
        Tabs.BorderColor3 = Color3.new(0,0,0)
        Tabs.BorderSizePixel = 0
        Tabs.Size = UDim2.new(0.2,0,1,0)
    end

    local UIListLayout = Instance.new("UIListLayout",Tabs) do
        UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        UIListLayout.Padding = UDim.new(0.025,0)
        UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    end
    local Barrier = Instance.new("Frame",Tabs) do
        Barrier.Name = "Barrier"
        Barrier.BackgroundColor3 = Color3.new(1,1,1)
        Barrier.BorderColor3 = Color3.new(0,0,0)
        Barrier.BorderSizePixel = 0
        Barrier.LayoutOrder = -100
    end
    local Pages = Instance.new("Frame",Container_1) do
        Pages.Name = "Pages"
        Pages.BackgroundColor3 = Color3.new(1,1,1)
        Pages.BackgroundTransparency = 1
        Pages.BorderColor3 = Color3.new(0,0,0)
        Pages.BorderSizePixel = 0
        Pages.Position = UDim2.new(0.2,0,0,0)
        Pages.Size = UDim2.new(0.8,0,1,0)
    end
    local Toggle = Instance.new("TextButton",PK_Alpha) do
        Toggle.Name = "Toggle"
        Toggle.AnchorPoint = Vector2.new(0.5,0.5)
        Toggle.AutoButtonColor = false
        Toggle.BackgroundColor3 = Color3.new(0.118,0.118,0.118)
        Toggle.BackgroundTransparency = 0.1
        Toggle.BorderColor3 = Color3.new(0,0,0)
        Toggle.BorderSizePixel = 0
        Toggle.FontFace = Font.new("rbxasset://fonts/families/Inconsolata.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Toggle.Position = UDim2.new(0.5,0,0.061,0)
        Toggle.Size = UDim2.new(0.075,0,0.075,0)
        Toggle.Text = ""
        Toggle.TextColor3 = Color3.new(1,1,1)
        Toggle.TextScaled = true
        Toggle.TextSize = 14
        Toggle.TextWrapped = true
    end

    local UICorner_4 = Instance.new("UICorner",Toggle) do
        UICorner_4.BottomLeftRadius = UDim.new(1,0)
        UICorner_4.BottomRightRadius = UDim.new(1,0)
        UICorner_4.CornerRadius = UDim.new(1,0)
        UICorner_4.TopLeftRadius = UDim.new(1,0)
        UICorner_4.TopRightRadius = UDim.new(1,0)
    end
    local UIAspectRatioConstraint_3 = Instance.new("UIAspectRatioConstraint",Toggle)
    local Logo_1 = Instance.new("ImageLabel",Toggle) do
        Logo_1.Name = "Logo"
        Logo_1.BackgroundColor3 = Color3.new(1,1,1)
        Logo_1.BackgroundTransparency = 1
        Logo_1.BorderColor3 = Color3.new(0,0,0)
        Logo_1.BorderSizePixel = 0
        Logo_1.Image = "rbxassetid://122670157280030"
        Logo_1.ScaleType = Enum.ScaleType.Fit
        Logo_1.Size = UDim2.new(1,0,1,0)
    end

    PK_Alpha.Parent = CoreGui
    lib.CreatedUI = PK_Alpha
end

function lib.createTemplates()
    local Templates = Instance.new("Folder") do
        Templates.Name = "Templates"
    end

    local Tab = Instance.new("Frame",Templates) do
        Tab.Name = "Tab"
        Tab.BackgroundColor3 = Color3.new(1,1,1)
        Tab.BackgroundTransparency = 1
        Tab.BorderColor3 = Color3.new(0,0,0)
        Tab.BorderSizePixel = 0
        Tab.Size = UDim2.new(1,0,0.1,0)
    end

    local UICorner = Instance.new("UICorner",Tab) do
        UICorner.BottomLeftRadius = UDim.new(1,0)
        UICorner.BottomRightRadius = UDim.new(1,0)
        UICorner.CornerRadius = UDim.new(1,0)
        UICorner.TopLeftRadius = UDim.new(1,0)
        UICorner.TopRightRadius = UDim.new(1,0)
    end
    local UIStroke = Instance.new("UIStroke",Tab) do
        UIStroke.BorderStrokePosition = Enum.BorderStrokePosition.Inner
        UIStroke.Color = Color3.new(1,1,1)
        UIStroke.Thickness = 2
    end
    local Button = Instance.new("TextButton",Tab) do
        Button.Name = "Button"
        Button.BackgroundColor3 = Color3.new(1,1,1)
        Button.BackgroundTransparency = 1
        Button.BorderColor3 = Color3.new(0,0,0)
        Button.BorderSizePixel = 0
        Button.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json",Enum.FontWeight.Regular,Enum.FontStyle.Normal)
        Button.Size = UDim2.new(1,0,1,0)
        Button.Text = ""
        Button.TextColor3 = Color3.new(0,0,0)
        Button.TextSize = 14
    end
    local List = Instance.new("Frame",Tab) do
        List.Name = "List"
        List.BackgroundColor3 = Color3.new(1,1,1)
        List.BackgroundTransparency = 1
        List.BorderColor3 = Color3.new(0,0,0)
        List.BorderSizePixel = 0
        List.Size = UDim2.new(1,0,1,0)
    end

    local Icon = Instance.new("ImageLabel",List) do
        Icon.Name = "Icon"
        Icon.BackgroundColor3 = Color3.new(1,1,1)
        Icon.BackgroundTransparency = 1
        Icon.BorderColor3 = Color3.new(0,0,0)
        Icon.BorderSizePixel = 0
        Icon.Image = "rbxassetid://110193614869268"
        Icon.Size = UDim2.new(0.8,0,0.8,0)
    end

    local UIAspectRatioConstraint = Instance.new("UIAspectRatioConstraint",Icon)
    local Title = Instance.new("TextLabel",List) do
        Title.Name = "Title"
        Title.BackgroundColor3 = Color3.new(1,1,1)
        Title.BackgroundTransparency = 1
        Title.BorderColor3 = Color3.new(0,0,0)
        Title.BorderSizePixel = 0
        Title.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Title.Size = UDim2.new(0.6,0,0.7,0)
        Title.Text = "Home"
        Title.TextColor3 = Color3.new(1,1,1)
        Title.TextScaled = true
        Title.TextSize = 14
        Title.TextWrapped = true
        Title.TextXAlignment = Enum.TextXAlignment.Left
    end
    local UIListLayout = Instance.new("UIListLayout",List) do
        UIListLayout.FillDirection = Enum.FillDirection.Horizontal
        UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        UIListLayout.Padding = UDim.new(0.025,0)
        UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
        UIListLayout.VerticalAlignment = Enum.VerticalAlignment.Center
    end
    local Page = Instance.new("ScrollingFrame",Templates) do
        Page.Name = "Page"
        Page.Active = true
        Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
        Page.BackgroundColor3 = Color3.new(1,1,1)
        Page.BackgroundTransparency = 1
        Page.BorderColor3 = Color3.new(0,0,0)
        Page.BorderSizePixel = 0
        Page.BottomImage = "rbxassetid://3062505976"
        Page.CanvasSize = UDim2.new(0,0,0,0)
        Page.MidImage = "rbxassetid://3062506202"
        Page.ScrollBarThickness = 8
        Page.Size = UDim2.new(1,0,1,0)
        Page.TopImage = "rbxassetid://3062506445"
    end

    local UIListLayout_1 = Instance.new("UIListLayout",Page) do
        UIListLayout_1.HorizontalAlignment = Enum.HorizontalAlignment.Center
        UIListLayout_1.Padding = UDim.new(0.025,0)
        UIListLayout_1.SortOrder = Enum.SortOrder.LayoutOrder
    end
    local Barrier = Instance.new("Frame",Page) do
        Barrier.Name = "Barrier"
        Barrier.BackgroundColor3 = Color3.new(1,1,1)
        Barrier.BackgroundTransparency = 1
        Barrier.BorderColor3 = Color3.new(0,0,0)
        Barrier.BorderSizePixel = 0
        Barrier.LayoutOrder = -100
    end
    local Block = Instance.new("Frame",Templates) do
        Block.Name = "Block"
        Block.AutomaticSize = Enum.AutomaticSize.Y
        Block.BackgroundColor3 = Color3.new(0.118,0.118,0.118)
        Block.BackgroundTransparency = 0.1
        Block.BorderColor3 = Color3.new(0,0,0)
        Block.BorderSizePixel = 0
        Block.Size = UDim2.new(0.9,0,0,0)
    end

    local UICorner_1 = Instance.new("UICorner",Block) do
        UICorner_1.BottomLeftRadius = UDim.new(0,20)
        UICorner_1.BottomRightRadius = UDim.new(0,20)
        UICorner_1.CornerRadius = UDim.new(0,20)
        UICorner_1.TopLeftRadius = UDim.new(0,20)
        UICorner_1.TopRightRadius = UDim.new(0,20)
    end
    local UIListLayout_2 = Instance.new("UIListLayout",Block) do
        UIListLayout_2.HorizontalAlignment = Enum.HorizontalAlignment.Center
        UIListLayout_2.Padding = UDim.new(0,10)
        UIListLayout_2.SortOrder = Enum.SortOrder.LayoutOrder
    end
    local Top = Instance.new("TextLabel",Block) do
        Top.Name = "Top"
        Top.BackgroundColor3 = Color3.new(1,1,1)
        Top.BackgroundTransparency = 1
        Top.BorderColor3 = Color3.new(0,0,0)
        Top.BorderSizePixel = 0
        Top.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Top.LayoutOrder = -99
        Top.Size = UDim2.new(1,0,0,30)
        Top.Text = "Visuals"
        Top.TextColor3 = Color3.new(1,1,1)
        Top.TextScaled = true
        Top.TextSize = 14
        Top.TextWrapped = true
        Top.TextYAlignment = Enum.TextYAlignment.Bottom
    end

    local Line = Instance.new("Frame",Top) do
        Line.Name = "Line"
        Line.BackgroundColor3 = Color3.new(1,1,1)
        Line.BorderColor3 = Color3.new(0,0,0)
        Line.BorderSizePixel = 0
        Line.LayoutOrder = -98
        Line.Position = UDim2.new(0,0,1,0)
        Line.Size = UDim2.new(1,0,0,1)
    end
    local Min = Instance.new("TextButton",Top) do
        Min.Name = "Min"
        Min.AnchorPoint = Vector2.new(1,0.5)
        Min.BackgroundColor3 = Color3.new(1,1,1)
        Min.BackgroundTransparency = 1
        Min.BorderColor3 = Color3.new(0,0,0)
        Min.BorderSizePixel = 0
        Min.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Min.Position = UDim2.new(0.975,0,0.5,0)
        Min.Size = UDim2.new(0.95,0,0.95,0)
        Min.Text = "–"
        Min.TextColor3 = Color3.new(1,1,1)
        Min.TextScaled = true
        Min.TextSize = 14
        Min.TextWrapped = true
    end

    local UICorner_2 = Instance.new("UICorner",Min)
    local UIAspectRatioConstraint_1 = Instance.new("UIAspectRatioConstraint",Min)
    local UIStroke_1 = Instance.new("UIStroke",Block) do
        UIStroke_1.Color = Color3.new(0.392,0.392,0.392)
    end
    local Barrier_1 = Instance.new("Frame",Block) do
        Barrier_1.Name = "Barrier"
        Barrier_1.BackgroundColor3 = Color3.new(1,1,1)
        Barrier_1.BackgroundTransparency = 1
        Barrier_1.BorderColor3 = Color3.new(0,0,0)
        Barrier_1.BorderSizePixel = 0
        Barrier_1.LayoutOrder = 50
        Barrier_1.Size = UDim2.new(1,0,0,10)
    end
    local Button_1 = Instance.new("Frame",Templates) do
        Button_1.Name = "Button"
        Button_1.BackgroundColor3 = Color3.new(1,1,1)
        Button_1.BackgroundTransparency = 0.9
        Button_1.BorderColor3 = Color3.new(0,0,0)
        Button_1.BorderSizePixel = 0
        Button_1.Position = UDim2.new(0.048,0,0.396,0)
        Button_1.Size = UDim2.new(0.95,0,0,35)
    end

    local UICorner_3 = Instance.new("UICorner",Button_1) do
        UICorner_3.BottomLeftRadius = UDim.new(1,0)
        UICorner_3.BottomRightRadius = UDim.new(1,0)
        UICorner_3.CornerRadius = UDim.new(1,0)
        UICorner_3.TopLeftRadius = UDim.new(1,0)
        UICorner_3.TopRightRadius = UDim.new(1,0)
    end
    local UIStroke_2 = Instance.new("UIStroke",Button_1) do
        UIStroke_2.Color = Color3.new(0.196,0.196,0.196)
    end
    local Title_1 = Instance.new("TextLabel",Button_1) do
        Title_1.Name = "Title"
        Title_1.AnchorPoint = Vector2.new(0,0.5)
        Title_1.BackgroundColor3 = Color3.new(1,1,1)
        Title_1.BackgroundTransparency = 1
        Title_1.BorderColor3 = Color3.new(0,0,0)
        Title_1.BorderSizePixel = 0
        Title_1.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Title_1.Position = UDim2.new(0.025,0,0.5,0)
        Title_1.Size = UDim2.new(0.7,0,0.8,0)
        Title_1.Text = "Player ESP"
        Title_1.TextColor3 = Color3.new(1,1,1)
        Title_1.TextScaled = true
        Title_1.TextSize = 14
        Title_1.TextWrapped = true
        Title_1.TextXAlignment = Enum.TextXAlignment.Left
    end
    local Toggle = Instance.new("TextButton",Button_1) do
        Toggle.Name = "Toggle"
        Toggle.AnchorPoint = Vector2.new(0,0.5)
        Toggle.BackgroundColor3 = Color3.new(0,0,0)
        Toggle.BackgroundTransparency = 0.75
        Toggle.BorderColor3 = Color3.new(1,1,1)
        Toggle.BorderSizePixel = 0
        Toggle.FontFace = Font.new("rbxasset://fonts/families/Roboto.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Toggle.Position = UDim2.new(0.8,0,0.5,0)
        Toggle.Size = UDim2.new(0.175,0,0.8,0)
        Toggle.Text = ""
        Toggle.TextColor3 = Color3.new(1,1,1)
        Toggle.TextScaled = true
        Toggle.TextSize = 14
        Toggle.TextWrapped = true
        Toggle.Visible = false
    end

    local UICorner_4 = Instance.new("UICorner",Toggle) do
        UICorner_4.BottomLeftRadius = UDim.new(1,0)
        UICorner_4.BottomRightRadius = UDim.new(1,0)
        UICorner_4.CornerRadius = UDim.new(1,0)
        UICorner_4.TopLeftRadius = UDim.new(1,0)
        UICorner_4.TopRightRadius = UDim.new(1,0)
    end
    local Frame = Instance.new("Frame",Toggle) do
        Frame.AnchorPoint = Vector2.new(0.5,0.5)
        Frame.BackgroundColor3 = Color3.new(1,0.322,0.322)
        Frame.BorderColor3 = Color3.new(0,0,0)
        Frame.BorderSizePixel = 0
        Frame.Position = UDim2.new(0.2,0,0.5,0)
        Frame.Size = UDim2.new(0.8,0,0.8,0)
    end

    local UICorner_5 = Instance.new("UICorner",Frame) do
        UICorner_5.BottomLeftRadius = UDim.new(1,0)
        UICorner_5.BottomRightRadius = UDim.new(1,0)
        UICorner_5.CornerRadius = UDim.new(1,0)
        UICorner_5.TopLeftRadius = UDim.new(1,0)
        UICorner_5.TopRightRadius = UDim.new(1,0)
    end
    local UIAspectRatioConstraint_2 = Instance.new("UIAspectRatioConstraint",Frame)
    local Toggle_1 = Instance.new("BoolValue",Toggle) do
        Toggle_1.Name = "Toggle"
    end
    local Off = Instance.new("Frame",Toggle) do
        Off.Name = "Off"
        Off.AnchorPoint = Vector2.new(0.5,0.5)
        Off.BackgroundColor3 = Color3.new(1,0.322,0.322)
        Off.BorderColor3 = Color3.new(0,0,0)
        Off.BorderSizePixel = 0
        Off.Position = UDim2.new(0.2,0,0.5,0)
        Off.Size = UDim2.new(0.8,0,0.8,0)
        Off.Visible = false
    end

    local UICorner_6 = Instance.new("UICorner",Off) do
        UICorner_6.BottomLeftRadius = UDim.new(1,0)
        UICorner_6.BottomRightRadius = UDim.new(1,0)
        UICorner_6.CornerRadius = UDim.new(1,0)
        UICorner_6.TopLeftRadius = UDim.new(1,0)
        UICorner_6.TopRightRadius = UDim.new(1,0)
    end
    local UIAspectRatioConstraint_3 = Instance.new("UIAspectRatioConstraint",Off)
    local On = Instance.new("Frame",Toggle) do
        On.Name = "On"
        On.AnchorPoint = Vector2.new(0.5,0.5)
        On.BackgroundColor3 = Color3.new(0,0.902,0.463)
        On.BorderColor3 = Color3.new(0,0,0)
        On.BorderSizePixel = 0
        On.Position = UDim2.new(0.8,0,0.5,0)
        On.Size = UDim2.new(0.8,0,0.8,0)
        On.Visible = false
    end

    local UICorner_7 = Instance.new("UICorner",On) do
        UICorner_7.BottomLeftRadius = UDim.new(1,0)
        UICorner_7.BottomRightRadius = UDim.new(1,0)
        UICorner_7.CornerRadius = UDim.new(1,0)
        UICorner_7.TopLeftRadius = UDim.new(1,0)
        UICorner_7.TopRightRadius = UDim.new(1,0)
    end
    local UIAspectRatioConstraint_4 = Instance.new("UIAspectRatioConstraint",On)
    local Slider = Instance.new("Frame",Button_1) do
        Slider.Name = "Slider"
        Slider.BackgroundColor3 = Color3.new(1,1,1)
        Slider.BackgroundTransparency = 1
        Slider.BorderColor3 = Color3.new(0,0,0)
        Slider.BorderSizePixel = 0
        Slider.Position = UDim2.new(0.45,0,0,0)
        Slider.Size = UDim2.new(0.55,0,1,0)
        Slider.Visible = false
    end

    local Bar = Instance.new("Frame",Slider) do
        Bar.Name = "Bar"
        Bar.AnchorPoint = Vector2.new(0,0.5)
        Bar.BackgroundColor3 = Color3.new(0,0,0)
        Bar.BackgroundTransparency = 0.5
        Bar.BorderColor3 = Color3.new(0,0,0)
        Bar.BorderSizePixel = 0
        Bar.Position = UDim2.new(0,0,0.5,0)
        Bar.Size = UDim2.new(0.75,0,0.3,0)
    end

    local UICorner_8 = Instance.new("UICorner",Bar) do
        UICorner_8.BottomLeftRadius = UDim.new(1,0)
        UICorner_8.BottomRightRadius = UDim.new(1,0)
        UICorner_8.CornerRadius = UDim.new(1,0)
        UICorner_8.TopLeftRadius = UDim.new(1,0)
        UICorner_8.TopRightRadius = UDim.new(1,0)
    end
    local Bar_1 = Instance.new("Frame",Bar) do
        Bar_1.Name = "Bar"
        Bar_1.AnchorPoint = Vector2.new(0,0.5)
        Bar_1.BackgroundColor3 = Color3.new(1,1,1)
        Bar_1.BorderColor3 = Color3.new(0,0,0)
        Bar_1.BorderSizePixel = 0
        Bar_1.Position = UDim2.new(0,0,0.5,0)
        Bar_1.Size = UDim2.new(0.5,0,1,0)
    end

    local UICorner_9 = Instance.new("UICorner",Bar_1) do
        UICorner_9.BottomLeftRadius = UDim.new(1,0)
        UICorner_9.BottomRightRadius = UDim.new(1,0)
        UICorner_9.CornerRadius = UDim.new(1,0)
        UICorner_9.TopLeftRadius = UDim.new(1,0)
        UICorner_9.TopRightRadius = UDim.new(1,0)
    end
    local TextBox = Instance.new("TextBox",Slider) do
        TextBox.AnchorPoint = Vector2.new(1,0.5)
        TextBox.BackgroundColor3 = Color3.new(0,0,0)
        TextBox.BackgroundTransparency = 1
        TextBox.BorderColor3 = Color3.new(0,0,0)
        TextBox.BorderSizePixel = 0
        TextBox.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        TextBox.PlaceholderColor3 = Color3.new(1,1,1)
        TextBox.Position = UDim2.new(0.975,0,0.5,0)
        TextBox.Size = UDim2.new(0.2,0,1,0)
        TextBox.Text = "50"
        TextBox.TextColor3 = Color3.new(1,1,1)
        TextBox.TextScaled = true
        TextBox.TextSize = 30
        TextBox.TextWrapped = true
    end

    local UICorner_10 = Instance.new("UICorner",TextBox) do
        UICorner_10.BottomLeftRadius = UDim.new(0.4,0)
        UICorner_10.BottomRightRadius = UDim.new(0.4,0)
        UICorner_10.CornerRadius = UDim.new(0.4,0)
        UICorner_10.TopLeftRadius = UDim.new(0.4,0)
        UICorner_10.TopRightRadius = UDim.new(0.4,0)
    end
    local Button_2 = Instance.new("TextButton",Button_1) do
        Button_2.Name = "Button"
        Button_2.BackgroundColor3 = Color3.new(1,1,1)
        Button_2.BackgroundTransparency = 1
        Button_2.BorderColor3 = Color3.new(0,0,0)
        Button_2.BorderSizePixel = 0
        Button_2.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json",Enum.FontWeight.Regular,Enum.FontStyle.Normal)
        Button_2.Size = UDim2.new(1,0,1,0)
        Button_2.Text = ""
        Button_2.TextColor3 = Color3.new(0,0,0)
        Button_2.TextSize = 14
        Button_2.Visible = false
    end
    local Keybind = Instance.new("TextButton",Button_1) do
        Keybind.Name = "Keybind"
        Keybind.AnchorPoint = Vector2.new(1,0.5)
        Keybind.BackgroundColor3 = Color3.new(0,0,0)
        Keybind.BackgroundTransparency = 0.7
        Keybind.BorderColor3 = Color3.new(0,0,0)
        Keybind.BorderSizePixel = 0
        Keybind.FontFace = Font.new("rbxasset://fonts/families/Arial.json",Enum.FontWeight.Bold,Enum.FontStyle.Normal)
        Keybind.Position = UDim2.new(0.975,0,0.5,0)
        Keybind.Size = UDim2.new(0.2,0,1,0)
        Keybind.Text = ""
        Keybind.TextColor3 = Color3.new(1,1,1)
        Keybind.TextSize = 14
        Keybind.Visible = false
    end

    local UICorner_11 = Instance.new("UICorner",Keybind) do
        UICorner_11.BottomLeftRadius = UDim.new(1,0)
        UICorner_11.BottomRightRadius = UDim.new(1,0)
        UICorner_11.CornerRadius = UDim.new(1,0)
        UICorner_11.TopLeftRadius = UDim.new(1,0)
        UICorner_11.TopRightRadius = UDim.new(1,0)
    end

    lib.Templates = Templates
end

function lib.TabTween(tab)
    tab.Button.MouseEnter:Connect(function()
        TweenService:Create(tab, tweenInfo, {BackgroundTransparency = 0}):Play()
        TweenService:Create(tab.List.Title, tweenInfo, {TextColor3 = Color3.new(0,0,0)}):Play()
        TweenService:Create(tab.List.Icon, tweenInfo, {ImageColor3 = Color3.new(0,0,0)}):Play()
    end)
    tab.Button.MouseLeave:Connect(function()
       if lib.SelectedTab ~= tab.Name then
            TweenService:Create(tab, tweenInfo, {BackgroundTransparency = 1}):Play()
            TweenService:Create(tab.List.Title, tweenInfo, {TextColor3 = Color3.new(1,1,1)}):Play()
            TweenService:Create(tab.List.Icon, tweenInfo, {ImageColor3 = Color3.new(1,1,1)}):Play()
        end
    end)
end

function lib.UpdateTab(name)
    local tab = nil

    for _, v in lib.Tabs do
        if v.Name and v.Name == name then
            tab = v
            break
        end
    end

    if tab and lib.SelectedTab == name then
        TweenService:Create(tab, tweenInfo, {BackgroundTransparency = 0}):Play()
        TweenService:Create(tab.List.Title, tweenInfo, {TextColor3 = Color3.new(0,0,0)}):Play()
        TweenService:Create(tab.List.Icon, tweenInfo, {ImageColor3 = Color3.new(0,0,0)}):Play()
    else
        TweenService:Create(tab, tweenInfo, {BackgroundTransparency = 1}):Play()
        TweenService:Create(tab.List.Title, tweenInfo, {TextColor3 = Color3.new(1,1,1)}):Play()
        TweenService:Create(tab.List.Icon, tweenInfo, {ImageColor3 = Color3.new(1,1,1)}):Play()
    end
end

function lib.newTab(name, image)
    if not lib.CreatedUI and not lib.Templates then return end

    local tab = lib.Templates.Tab:Clone()
    tab.Name = name
    tab.LayoutOrder = #lib.CreatedUI.Main.Container.Tabs:GetChildren() + 1
    tab.List.Title.Text = name
    tab.List.Icon.Image = "rbxassetid://"..image
    tab.Parent = lib.CreatedUI.Main.Container.Tabs
    tab.Visible = true

    table.insert(lib.Tabs, tab)

    tab.Button.Activated:Connect(function()
        lib.switchPage(name)
    end)

    lib.TabTween(tab)
    lib.newPage(name)
end

function lib.newPage(name)
    if not lib.CreatedUI and not lib.Templates then return end

    local page = lib.Templates.Page:Clone()
    page.Name = name
    page.Parent = lib.CreatedUI.Main.Container.Pages
    page.Visible = false

    table.insert(lib.Pages, page)
end

function lib.switchPage(name)
    if not lib.CreatedUI and not lib.Templates then return end

    lib.SelectedTab = name
    lib.UpdateTab(name)

    for _, page in lib.Pages do
        if page.Name ~= name then
            page.Visible = false
        else
            page.Visible = true
        end
    end
end

function lib.newBlock(name, pageName)
    if not lib.CreatedUI and not lib.Templates then return end

    local block = lib.Templates.Block:Clone()
    block.Name = name
    block.LayoutOrder = #lib.CreatedUI.Main.Container.Pages[pageName]:GetChildren() + 1
    block.Top.Text = name
    block.Parent = lib.CreatedUI.Main.Container.Pages[pageName]
    block.Visible = true
    lib.Blocks[name] = {
        Size = 1,
        Element = block,
        Page = pageName
    }

    block.Top.Min.Activated:Connect(function()
        local blockData = lib.Blocks[name]
        if blockData then
            if blockData.Size == 1 then
                blockData.Element.Top.Min.Text = "+"
                blockData.Size = 0

                for _, btn in lib.Buttons do
                    if btn.Block == name then
                        btn.Element.Visible = false
                    end
                end
            else
                blockData.Element.Top.Min.Text = "–"
                blockData.Size = 1

                for _, btn in lib.Buttons do
                    if btn.Block == name then
                        btn.Element.Visible = true
                    end
                end
            end
        end
    end)
end

function lib.newButton(name, block, keybind, callback)
    if not lib.CreatedUI and not lib.Templates then return end

    local blockData = lib.Blocks[block]
    if not blockData then return end

    local button = lib.Templates.Button:Clone()
    button.LayoutOrder = #lib.CreatedUI.Main.Container.Pages[blockData.Page][block]:GetChildren() + 1
    button.Name = name
    button.Title.Text = name
    button.Button.Visible = true
    button.Keybind.Visible = keybind
    button.Parent = lib.CreatedUI.Main.Container.Pages[blockData.Page][block]

    lib.Buttons[name] = {
        Block = block,
        Keybind = nil,
        Type = "Button",
        Element = button
    }

    button.Button.Activated:Connect(function()
        callback()
    end)

    if keybind then
        local listening = false
        local currentKey = nil

        local function setKey(key)
            currentKey = key
            lib.Buttons[name].Keybind = key
            button.Keybind.Text = key and key.Name or "None"
        end

        setKey(nil)

        button.Keybind.Activated:Connect(function()
            if listening then return end
            listening = true
            button.Keybind.Text = "..."

            local con
            con = UserInputService.InputBegan:Connect(function(input, gpe)
                if gpe then return end
                if input.UserInputType ~= Enum.UserInputType.Keyboard then return end

                if input.KeyCode == Enum.KeyCode.Escape then
                    setKey(nil)
                else
                    setKey(input.KeyCode)
                end

                listening = false
                con:Disconnect()
            end)
        end)

        UserInputService.InputBegan:Connect(function(input, gpe)
            if gpe or listening or not currentKey then return end
            if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
            if input.KeyCode == currentKey then
                callback()
            end
        end)
    end
end

function lib.newToggle(name, block, callback)
    if not lib.CreatedUI and not lib.Templates then return end

    local blockData = lib.Blocks[block]
    if not blockData then return end

    local button = lib.Templates.Button:Clone()
    button.LayoutOrder = #lib.CreatedUI.Main.Container.Pages[blockData.Page][block]:GetChildren() + 1
    button.Name = name
    button.Title.Text = name
    button.Toggle.Visible = true
    button.Parent = lib.CreatedUI.Main.Container.Pages[blockData.Page][block]

    lib.Buttons[name] = {
        Block = block,
        Toggle = false,
        Type = "Toggle",
        Element = button
    }

    button.Toggle.Activated:Connect(function()
        local buttonData = lib.Buttons[name]
        if buttonData then
            if buttonData.Toggle then
                buttonData.Toggle = false

                TweenService:Create(button.Toggle.Frame, tweenInfo, {Position = button.Toggle.Off.Position, BackgroundColor3 = button.Toggle.Off.BackgroundColor3}):Play()

                callback(buttonData.Toggle)
            else
                buttonData.Toggle = true

                TweenService:Create(button.Toggle.Frame, tweenInfo, {Position = button.Toggle.On.Position, BackgroundColor3 = button.Toggle.On.BackgroundColor3}):Play()

                callback(buttonData.Toggle)
            end
        end
    end)
end

function lib.playSound(soundID)
    local sound = Instance.new("Sound")
	sound.SoundId = "rbxassetid://" .. soundID
	sound.Parent = SoundService
	sound:Play()
	Debris:AddItem(sound, 3)
end

return lib
